

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Battle</title>
    <link href="main.css" type="text/css" rel="stylesheet">
  </head>

  <body>
    <div class="content" >
      <main class="screen1">
        <fieldset class="screen2">
          <h1>Battle <span>MAKERS</span></h1>
          <h3><%= $game.player_name(1) %> vs <%= $game.player_name(2) %></h3>
          <p><%= $game.player_name($game.current_player) %>'s turn: make a move!</p>
          <p>Player 1s HP = <%= $game.player_hp(1) %><br>
            Player 2s HP = <%= $game.player_hp(2) %><br></p>
            <% if $game.move_last != nil %>
            <p><%= $game.player_name($game.current_player) %> <%= $game.move_last %></p>
            <% end %>
            <form class="act_first_but" action="/attack" method="post">
              <div>
                <input id="attack_but" type="submit" value="Attack">
              </div>
            </form>
          </fieldset>
        </main>

    </div>
  </body>
</html>
